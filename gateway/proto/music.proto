syntax = "proto3";

package music;

import "google/protobuf/empty.proto";

option go_package = "github.com/umarbek-backend-engineer/Music_Player/gateway/proto/gen;pb";

service MusicService {
    rpc ListMusic(google.protobuf.Empty) returns (ListResponse);
    rpc UploadMusic(stream UploadMusicChunks) returns (UploadMusicResponse);
    rpc StreamMusic(StreamRequest) returns (stream MusicChunk);
}

message UploadMusicResponse {
    string status = 1;
}

message UploadMusicChunks {
    string filename = 1;
    bytes data = 2;
}

message MusicItem {
    string id = 1;
    string filename = 2;
}

message ListResponse {
    repeated MusicItem songs = 1;
}

message StreamRequest {
    string id = 1;
}

message MusicChunk {
    string name = 1;
    bytes content = 2;
}
